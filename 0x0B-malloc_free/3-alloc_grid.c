#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * **alloc_grid - returns a pointer to a 2D array of integers
 * @width: width of grid
 * @height: height of grid
 * Return: Null on failure, or if width or height are negative or 0
 */
int **alloc_grid(int width, int height)
{


	if (width <= 0 || height <= 0)
		return (NULL);
}
